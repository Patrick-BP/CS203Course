package Question6;

public class Arrays {

    /* Yes, you cannot change the size of an array once you define it.
       Yes, you insert an element after creating an array.
       No, you cannot delete an element in an array, you can only replace it with null or 0 depending on the type of the array;
     */
}
