package Question1;

public class Q1 {
    /*Identify problems with this code:

    public int searchAccount( int[25] number ) {   //<======= bad declaration of the parameter (int[] number)
        number = new int[15];
        for (int i = 0; i < number.length; i++ )
            number[i] = number[i-1] + number[i+1];
        return number;  // <========= the return type should be an int as we define in the method signature
    }*/
}
